{% extends 'base.html.twig' %}

{% block title %}wf3tm posts detail{% endblock %}

{% block body %}


	<div class="container">

		
            {{ post.getCreationDate()| date('Y-m-d H:i:s') }}
            <br>
            {{ post.getCategory() }} 
            <br>
            {{ post.getTitle() }}
            <br>
            {{ post.getText() }}   

            {% if post.getPicture() is not empty %}
				<td><img src="{{ path('download_document', {'document' : post.getPicture().getId()}) }}"></td>
			{% else %}
				<td></td>
			{% endif %}			 

			 {% if post.getPicture() is not empty %}
                <td><img src="{{ path('download_document', {'document' : post.getPicture().getId()}) }}"></td>
            {% else %}
                <td></td>
            {% endif %}
			 


		<p>id : {{ post.getId() }}</p>
		<p>created date : {{ post.getCreationDate()|date('Y-m-d H:i:s') }}</p>
		<p>priority : {{ post.getText() }}</p>
		<p>title : {{ post.getTitle() }}</p>

		<div class="element-warning mt-5">
		</div>
		<div id="posting">
       {{ form(commentForm) }}
    </div>
	</div>
{% endblock %}
